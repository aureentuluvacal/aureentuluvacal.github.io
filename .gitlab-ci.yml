image: node:latest

pages:
  script:
    - npm install
    - npm install gatsby-cli
    - npm audit fix
    - node_modules/.bin/gatsby build
  artifacts:
    paths:
      - public
  cache:
    paths:
      - node_modules
  only:
    - master
